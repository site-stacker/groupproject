
const functions = {
    //Jon's Tests
    fonts: (arr) =>{
        arr === true
    }
    
    //Michael

    //Seba

    toggle: (bool) => !bool,
    checkSection: (s) => s === "About Us" ? "About Us": s === "Features" ? "Features" : "Header"

}

module.exports = functions